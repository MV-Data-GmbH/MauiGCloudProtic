<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
            xmlns:templates="clr-namespace:GCloudPhone.Views.Templates"
             x:Class="GCloudPhone.Views.NFCReaderPage"
             Title="" NavigationPage.HasNavigationBar="False">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="5*"></RowDefinition>
            <RowDefinition Height="95*"></RowDefinition>
        </Grid.RowDefinitions>

        <templates:BackButtonControl/>
        
        
        
        <StackLayout Grid.Row="1" Margin="15,10,15,10" VerticalOptions="Center">
            
            <Label x:Name="InstructionLabel" Text="Bitte halten Sie Ihr Telefon in die Nähe eines NFC-Tags."
                   HorizontalOptions="Center"
                   VerticalOptions="CenterAndExpand" 
                   HorizontalTextAlignment="Center" FontSize="Large"/>
            
            <Image Source="nfc.jpg" Style="{StaticResource CenteredImages}"/>
            
            <!--<Button Text="NFC-Scan starten" Clicked="StartNfcScanning" />-->
        </StackLayout>


    </Grid>
</ContentPage>