<?xml version="1.0" encoding="utf-8" ?>
<toolkit:Popup xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             x:Class="GCloudPhone.Views.Shop.OrderProcesses.RepeatOrderPopup">
    <VerticalStackLayout Padding="5" BackgroundColor="White">
        <Label Text="Möchten Sie die Bestellung wiederholen?" FontSize="Medium"  HorizontalOptions="Center" Margin="20"/>
        <Grid 
Padding="30">
            <Grid.ColumnDefinitions> 
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Button Text="Nein" 
                    Clicked="OnNoClicked" 
                    Style="{StaticResource DeclineButtonStyle}"
                    WidthRequest="90" 
                    HorizontalOptions="Center"/>
            <Button Grid.Column="1" 
                    Text="Ja" 
                    Style="{StaticResource ConfirmButtonStyle}"
                    Clicked="OnYesClicked" WidthRequest="90"  
                    HorizontalOptions="Center"/>
        </Grid>
    </VerticalStackLayout>
</toolkit:Popup>
